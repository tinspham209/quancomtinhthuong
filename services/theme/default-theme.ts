import { ThemeConfig } from './types';

export const defaultTheme: ThemeConfig = {
  common: {
    bgColor: '#fff',
    color: '#000',
    primaryBtnBgColor: '#fff',
    primaryBtnColor: '#000',
  },
  card: {
    bgColor: '#fff',
    color: '#000',
  },
};
